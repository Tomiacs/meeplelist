{
  "name": "meeplelist",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "node scripts/preview.mjs",
    "test": "node --test",
    "update:data": "node scripts/update-data.mjs",
    "seed:cache": "node scripts/seed-from-cache.mjs"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.5"
  }
}
